import styled from 'styled-components';

const StyledForm = styled.form`
	padding: 2rem;
	width: 800px;
	min-height: 500px;

	border-radius: 25px;
	box-shadow: var(--shadow);
`;

export { StyledForm };
