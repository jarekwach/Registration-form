import React from 'react';
import { StyledInputText } from './InputText.styled';

const InputText = (props) => {
	return <StyledInputText type='text'></StyledInputText>;
};

export default InputText;
